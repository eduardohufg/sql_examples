-- Content: Average age of all users
-- La sentencia AVG se usa para encontrar el promedio de los valores de una columna
-- se coloca despues de SELECT y antes de FROM
-- podemos usar AVG(columna) para encontrar el promedio de los valores de una columna
-- tambien podemos usar AVG() con cualquier columna numerica
-- podemos usar AVG() con cualquier operador de comparacion

SELECT SUM(age) FROM users