-- La sentencia SUM se usa para sumar los valores de una columna
-- se coloca despues de SELECT y antes de FROM
-- podemos usar SUM(columna) para sumar los valores de una columna
-- tambien podemos usar SUM() con cualquier columna numerica
-- podemos usar SUM() con cualquier operador de comparacion

SELECT SUM(age) FROM users;
