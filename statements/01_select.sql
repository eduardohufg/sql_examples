-- la sentencia select se usa para seleccionar datos
-- ponemos SELECT seguido de los daros que queremos consultar
-- y FROM para indicar la tabla que queremos consultar 
SELECT * FROM users;

SELECT name FROM users;

SELECT name, email FROM users;